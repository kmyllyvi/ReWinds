-- Migration from schema version 2 to 3
-- Adds AppSettings key-value table for KIM-140 (Days of Interest feature)

CREATE TABLE AppSettings (
    key   TEXT NOT NULL PRIMARY KEY,
    value TEXT NOT NULL
);
