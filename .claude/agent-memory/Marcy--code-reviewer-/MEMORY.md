# Marcy (Code Reviewer) Memory Index

- [FK cascade not enforced](project_fk_cascade_not_enforced.md) — ON DELETE CASCADE declared in schema but drivers never enable PRAGMA foreign_keys; cascade deletes orphan children at runtime
- [Linear labels by ID](feedback_linear_labels_by_id.md) — save_issue labels field takes IDs not names; passing names silently wipes all labels
