# Copy to .env and fill in a real key. .env is gitignored; this example is not.
# Personal API key from Linear: Settings -> API -> Personal API keys.
# Used by scripts/export-linear-done.ts (Linear housekeeping, not the app build).
LINEAR_API_KEY=lin_api_xxx
